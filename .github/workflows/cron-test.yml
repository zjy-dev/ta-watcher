name: Cron Test (删除我)

on:
  schedule:
    # 测试：5分钟后运行（请在测试后删除此文件）
    - cron: "24 09 * * *"

jobs:
  test-cron:
    runs-on: ubuntu-latest
    steps:
      - name: Test cron scheduling
        run: |
          echo "🎉 Cron scheduling works!"
          echo "Current time: $(date)"
          echo "This proves that scheduled workflows are working."
          echo "⚠️ Remember to delete this test workflow file after verification."
